{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": [
    "eslint",
    "typescript",
    "unicorn",
    "oxc",
    "vue"
  ],
  "env": {
    "browser": true
  },
  "categories": {
    "correctness": "error"
  },
  // "ignorePatterns": "Ignore generated Vue virtual files (*.vue.js) that only exist in CI/type-check output so local and CI oxlint behavior match.",
  "ignorePatterns": [
    "**/*.vue.js"
  ]
}